# Store Item Demand Forecasting Challenge

🔗 **Соревнование:** [Store Item Demand Forecasting (Kaggle)](https://www.kaggle.com/competitions/demand-forecasting-kernels-only)

---

## 1. Описание задачи

Проект решает задачу прогнозирования ежедневных продаж (`sales`) на ближайшие **3 месяца** для каждой пары **магазин–товар**:

- 10 магазинов × 50 товаров = **500 временных рядов**;
- 5 лет истории продаж (train) + 3 месяца для предсказаний (test);
- тип задачи — **регрессия на временных рядах**.

**Бизнес-цель:** снизить издержки и улучшить управление запасами за счёт более точных прогнозов:

- избежать дефицита и перепроизводства;
- оптимизировать закупки и логистику;
- уменьшить замороженный капитал на складе.

---

## 2. Данные

В оригинальном соревновании используются три основных файла:

- `data/raw/train.csv` — обучающий датасет за 5 лет;
- `data/raw/test.csv` — период 3 месяцев, по которому нужно предсказать `sales`;
- `data/raw/sample_submission.csv` — пример формата отправки.

**Поля:**

- `date` — дата продажи (ежедневные наблюдения);
- `store` — ID магазина (1–10);
- `item` — ID товара (1–50);
- `sales` — количество проданных единиц товара (**целевой признак**, только в `train`).

**Target:** `sales`.

---

## 3. Метрика качества

Основная метрика — **SMAPE (Symmetric Mean Absolute Percentage Error)**:

\[
\text{SMAPE} = \frac{100\%}{N} \sum_{i=1}^N \frac{|y_i - \hat{y}_i|}{(|y_i| + |\hat{y}_i|)/2}
\]

Особенности:

- если `y = 0` и `ŷ = 0`, вклад в SMAPE = 0;
- чем ниже SMAPE, тем лучше;
- симметрично штрафует завышение и занижение прогноза;
- хорошо подходит для продаж с разной амплитудой и выраженной сезонностью.

Целевые уровни (по памяти из `memory-bank`):

- baseline: SMAPE ~20–25%;
- Iteration 1: SMAPE ~15–18%;
- цель проекта: SMAPE < 15%, stretch goal — < 13%.

---

## 4. Архитектура системы

Высокоуровневая схема:

```text
Raw Data → Preprocessing → Feature Engineering → Model Training → Inference (API) → UI → Predictions
   ↓           ↓                  ↓                   ↓                ↓           ↓        ↓
 CSV files   cleaned data    engineered features   saved model      FastAPI     Streamlit  submission
```

Основные слои:

- **Data Layer**
  - загрузка `train/test/sample_submission`;
  - базовая валидация схемы и типов;
  - разбиение по времени (train/val/test).

- **Feature Layer**
  - временные признаки: year, month, week, dayofweek, и др.;
  - лаги: `sales_lag_7`, `sales_lag_14`, `sales_lag_30`, `sales_lag_365`;
  - rolling-статистики: `rolling_mean_7`, `rolling_std_7`, `rolling_mean_30`;
  - encoding `store/item` под tree-модели;
  - строгая защита от data leakage (`shift()` перед rolling и лагами).

- **Model Layer**
  - baseline-модели (наивный прогноз, moving average);
  - основная модель: **LightGBM Global Model** (одна модель на все `store×item`);
  - сохранение артефактов:
    - `artifacts/models/lightgbm_smape_11.74.joblib` — модель;
    - `artifacts/models/lightgbm_smape_11.74.json` — метаданные и фичи.

- **Inference Layer**
  - загрузка модели и метаданных;
  - lookup по предвычисленным фичам:
    - `data/processed/test_features_cleaned.csv` с индексом по (`date`, `store`, `item`);
  - генерация предсказаний для батчей запросов.

- **API Layer (FastAPI)**
  - `POST /predict` — batch-прогноз для списка `(date, store, item)`;
  - `GET /health` — health-check;
  - обёртка над LightGBM + lookup по фичам.

- **UI Layer (Streamlit)**
  - Страница **«Прогноз»**:
    - single-прогноз по введённым `date/store/item`;
    - batch-прогноз по загруженному CSV (`date,store,item`);
    - интеграция с FastAPI `/predict`.
  - Страница **«О модели»**:
    - базовая информация и метрики из `lightgbm_smape_11.74.json`.

---

## 5. Структура репозитория

Фактическая структура (укрупнённо):

```text
├── data/
│   ├── raw/                       # Исходные данные Kaggle (train/test/sample_submission)
│   └── processed/                 # Обработанные фичи (train_features*, test_features*)
│
├── notebooks/                     # Jupyter-ноутбуки
│   ├── 01_comprehensive_eda_and_validation.ipynb
│   └── 02_model_training.ipynb
│
├── src/
│   ├── data/
│   │   ├── __init__.py
│   │   ├── load_data.py           # Загрузка raw-данных
│   │   ├── save_data.py           # Сохранение/загрузка обработанных данных
│   │   └── README.md              # Документация data-модуля
│   │
│   ├── features/
│   │   ├── __init__.py
│   │   ├── build_features.py      # Генерация временных фичей, лагов, rolling и др.
│   │   ├── validation.py          # Валидация фичей (корреляции, leakage-check)
│   │   ├── example_usage.py       # Примеры использования feature-пайплайна
│   │   └── README.md              # Документация feature-модуля
│   │
│   ├── models/
│   │   ├── __init__.py
│   │   ├── train.py               # Обучение моделей (LightGBM и др.)
│   │   └── metrics.py             # SMAPE, RMSE, MAE, R²
│   │
│   └── app/                       # Продакшен-инференс (FastAPI)
│       ├── __init__.py
│       ├── main.py                # FastAPI-приложение (/predict, /health)
│       ├── models.py              # Pydantic-схемы запросов/ответов
│       ├── predictor.py           # Класс SalesPredictor (LightGBM + lookup по фичам)
│       └── config.py              # Пути к моделям/данным, конфиг проекта
│
├── artifacts/
│   ├── models/                    # Сохранённые модели и метаданные
│   └── submissions/               # Kaggle submissions
│
├── ui/
│   ├── page_forecast.py           # Страница Streamlit «Прогноз»
│   ├── page_about.py              # Страница «О модели»
│   ├── api_client.py              # HTTP-клиент к FastAPI
│   └── state.py                   # Работа с st.session_state
│
├── memory-bank/                   # Project brief, context, tech/system patterns, progress
│
├── streamlit_app.py               # Точка входа для Streamlit
├── Dockerfile                     # Docker-образ для FastAPI-сервиса
├── requirements.txt               # Зависимости (ML + FastAPI + Streamlit)
└── README.md                      # Этот файл

## 6. Как запустить

### 6.1. Подготовка окружения

python -m venv .venv
.venv\Scripts\activate  # Windows

pip install -r requirements.txtПредполагается, что:

- `data/raw/` уже содержит `train.csv`, `test.csv`, `sample_submission.csv`;
- `artifacts/models/` содержит обученную модель `lightgbm_smape_11.74.joblib` и её `.json`;
- `data/processed/test_features_cleaned.csv` уже создан (через ноутбук EDA/feature engineering).

### 6.2. Запуск API (локально, без Docker)

uvicorn src.app.main:app --host 0.0.0.0 --port 8000После запуска:

- `http://localhost:8000/health` — статус сервиса;
- `http://localhost:8000/docs` — Swagger UI.

### 6.3. Запуск Streamlit UI

В другом терминале:

streamlit run streamlit_app.pyФункционал:

- на странице **«Прогноз»**:
  - ввести `date`, `store`, `item` и получить точечный прогноз;
  - загрузить CSV с колонками `date,store,item` и получить батч предсказаний;
- на странице **«О модели»**:
  - посмотреть тип модели, периоды обучения/валидации, базовые метрики и список фичей.

### 6.4. Запуск через Docker (API)

Сборка и запуск образа (пример):

docker build -t demand-forecasting-api:latest .
docker run --rm -p 8000:8000 demand-forecasting-api:latestДалее UI можно поднимать отдельно через `streamlit run` и ходить в Docker-контейнер по `http://localhost:8000`.

---

## 7. Статус проекта и планы

**Сделано:**

- End-to-end pipeline: данные → фичи → LightGBM-модель → FastAPI → Streamlit.
- Реализован безопасный feature engineering без data leakage.
- Настроен lookup по предвычисленным фичам `test_features_cleaned.csv` для быстрого инференса.
- Подготовлен Docker-образ для API и базовый фронтенд.

**В планах:**

- зафиксировать и улучшить метрики (уточнить SMAPE на валидации);
- добавить визуализации и агрегированные отчёты в Streamlit (графики прогноза, топ-N товаров/магазинов);
- добавить автотесты (API + core-функции) и более подробную документацию по деплою.
