# Store Item Demand Forecasting Challenge

🔗 **Соревнование:** [Store Item Demand Forecasting (Kaggle)](https://www.kaggle.com/competitions/demand-forecasting-kernels-only)

---

## 1. Описание задачи

Задача — спрогнозировать ежедневные продажи (`sales`) на ближайшие **3 месяца** для каждой пары **магазин–товар**.

- **Дано**: 5 лет истории продаж 50 товаров в 10 магазинах.
- **Цель**: построить модель, которая хорошо обобщается и даёт стабильный прогноз по всем 500 временным рядам.
- **Тип задачи**: регрессия на временных рядах (time series forecasting).
- **Бизнес-ценность**:
  - оптимизация запасов,
  - снижение издержек на хранение,
  - избегание дефицита и перепроизводства,
  - улучшение планирования закупок и логистики.

Kaggle позиционирует соревнование как площадку для изучения:

- моделирования сезонности и трендов;
- сравнения отдельных моделей по ряду vs глобальной модели на все ряды;
- сравнения классических моделей (ARIMA) и градиентного бустинга / deep learning.

---

## 2. Данные

На вкладке **Data** в соревновании доступны три основных файла:

- **`train.csv`** — обучающий датасет за 5 лет.
- **`test.csv`** — период следующих 3 месяцев, по которому нужно предсказать `sales`.
- **`sample_submission.csv`** — пример правильного формата отправки решения.

**Поля:**

- **`date`** — дата продажи (ежедневные наблюдения; без праздников/закрытий).
- **`store`** — ID магазина (1–10).
- **`item`** — ID товара (1–50).
- **`sales`** — количество проданных единиц товара в этот день (**целевой признак**).

**Target:** `sales`.

---

## 3. Метрика качества

Используется **SMAPE (Symmetric Mean Absolute Percentage Error)**:

\[
\text{SMAPE} = \frac{100\%}{N} \sum_{i=1}^N \frac{\left| y_i - \hat{y}_i \right|}{\left(\left| y_i \right| + \left| \hat{y}_i \right|\right) / 2}
\]

Особенности:

- Если `actual = 0` и `prediction = 0`, вклад в SMAPE считается равным 0.
- Чем **ниже** SMAPE, тем **лучше** модель.
- Ошибки завышения и занижения штрафуются **симметрично**.
- Метрика хорошо подходит для задач продаж с разной амплитудой и сезонностью.

---

## 4. Структура проекта

Проект ориентирован на разделение:

- экспериментов (**notebooks**) и прод-кода (**src**),
- данных,
- артефактов (модели, фичи),
- сабмитов для Kaggle.

Примерная структура:

├── data/
│   ├── raw/                # Оригинальные данные Kaggle (train.csv, test.csv, sample_submission.csv)
│   └── processed/          # Обработанные данные (фичи, агрегаты)
│
├── notebooks/
│   ├── 00_quick_eda_raw_data.ipynb           # Быстрый EDA на сырых данных
│   ├── 01_comprehensive_eda_features.ipynb   # Полный EDA на данных с фичами
│   ├── 02_baseline_models.ipynb
│   ├── 03_advanced_models.ipynb
│   └── 04_final_model.ipynb
│
├── src/
│   ├── data/
│   │   ├── load_data.py        # Функции загрузки данных
│   │   └── preprocess.py       # Скрипты преобразования данных
│   │
│   ├── features/
│   │   └── build_features.py   # Генерация фичей
│   │
│   ├── models/
│   │   ├── train.py            # Тренировка моделей
│   │   ├── predict.py          # Инференс / подготовка сабмита
│   │   └── utils.py            # Вспомогательные функции
│   │
│   ├── api/
│   │   ├── main.py             # FastAPI сервис
│   │   └── schemas.py          # Pydantic-схемы запросов/ответов
│   │
│   ├── config.py               # Настройки проекта
│   └── pipeline.py             # Сквозной pipeline инференса
│
├── artifacts/
│   ├── models/                 # Сохранённые модели (pickle, joblib)
│   ├── scalers/                # Стандартизаторы и энкодеры
│   └── features/               # Артефакты для фичей
│
├── submissions/                # Сгенерированные сабмиты для Kaggle
│
├── Dockerfile                  # Docker-образ сервиса API
├── requirements.txt            # Все зависимости проекта
└── README.md                   # Текущий файл---

## 5. Возможные дополнения

В README (или отдельных документах) можно добавить:

- **Список использованных моделей**: ARIMA, XGBoost, LightGBM, baseline-модели.
- **Анализ сезонности и трендов**:
  - недельная/месячная/годовая сезонность,
  - сравнение по магазинам и товарам.
- **Результаты экспериментов**:
  - таблица с метриками для разных моделей/конфигов,
  - графики ошибок.
- **Визуализации**:
  - временные ряды по отдельным товарам/магазинам,
  - сравнение факта и прогноза.
- **Лучшие настройки моделей**:
  - ключевые гиперпараметры,
  - краткая мотивация выбранных конфигов.

---

## 6. Заключение

Этот README даёт:

- контекст задачи,
- описание данных и метрики,
- общую структуру проекта.

Он служит входной точкой для новых участников и удобной шпаргалкой для дальнейшего развития (улучшение фичей, моделей, деплой через API/Docker, добавление Streamlit‑фронтенда и т.д.).# Store Item Demand Forecasting Challenge
🔗 Ссылка на соревнование: https://www.kaggle.com/competitions/demand-forecasting-kernels-only

1. Заголовок и Источник
Store Item Demand Forecasting Challenge
Источник: Kaggle Competitions
Ссылка: https://www.kaggle.com/competitions/demand-forecasting-kernels-only

2. Описание Задачи (Problem Statement)
Данное соревнование предлагает решить задачу прогнозирования временных рядов (time series forecasting).
Участникам предоставляется 5 лет данных о продажах 50 товаров в 10 магазинах.
Бизнес-задача:
Спрогнозировать объём продаж (sales) на ближайшие 3 месяца по каждому магазину и товару.
Такие прогнозы позволяют бизнесу эффективно управлять запасами, планированием, логистикой и предотвращать ситуации дефицита или перепроизводства.
Это задача регрессии на временных рядах, где нужно учитывать особенности сезонности, трендов, влияния магазина и товара, а также возможные взаимодействия между ними.
Kaggle подчёркивает, что данное соревнование создано для изучения подходов:
моделирование сезонности;


раздельное прогнозирование по магазинам или совместное обучение;


сравнение классических моделей (ARIMA) и современных (градиентный бустинг, deep learning).



3. Данные (Data)
На вкладке Data доступно три основных файла.
Файлы:
train.csv — обучающий датасет за 5 лет.


test.csv — данные, по которым нужно предсказать продажи (период — следующие 3 месяца).


Public/Private split — по времени (более поздние даты — private).


sample_submission.csv — пример правильного формата отправки решения.



Описание полей (data fields):
- date      \\ Дата продажи (ежедневные наблюдения). Нет праздников или закрытия магазинов.
- store     \\ ID магазина (1–10).
- item      \\ ID товара (1–50).
- sales     \\Количество проданных единиц товара в этот день. ← целевая переменная (target)

Target: sales

4. Метрика Оценки (Evaluation Metric)
В соревновании используется метрика:
SMAPE (Symmetric Mean Absolute Percentage Error)
SMAPE = \frac{100\%}{N} \sum_{i=1}^N \frac{|y_i - \hat{y}_i|}{(|y_i| + |\hat{y}_i|)/2}
Особые правила:
Если actual = 0 и prediction = 0, то вклад в SMAPE считается равным 0.


Как интерпретировать SMAPE в контексте задачи?
Показывает среднюю симметричную процентную ошибку прогноза.


Чем ниже значение SMAPE, тем лучше модель.


Симметричность означает, что ошибка завышения и занижения штрафуется одинаково.


Метрика идеальна для задач прогнозирования продаж, т.к. значения могут быть небольшими, сезонными и с разной амплитудой.

5. Структура Проекта (Project Structure)
Структура ориентирована на индустриальные стандарты:
разделение экспериментов (notebooks) и прод-кода (src),


хранение данных в отдельной папке,


артефакты и сабмиты вынесены отдельно,


готовность к Docker деплою.



Предлагаемая структура репозитория:
├── data/
│   ├── raw/                # Оригинальные данные Kaggle (train.csv, test.csv,sample_submission.csv)
│   └── processed/          # Обработанные данные (фичи, агрегаты)
│
├── notebooks/
│   ├── 00_quick_eda_raw_data.ipynb      # Быстрый EDA на сырых данных
│   ├── 01_comprehensive_eda_features.ipynb  # Полный EDA на данных с фичами
│   ├── 02_baseline_models.ipynb
│   ├── 03_advanced_models.ipynb
│   └── 04_final_model.ipynb
│
├── src/
│   ├── data/
│   │   ├── load_data.py        # Функции загрузки данных
│   │   └── preprocess.py       # Скрипты преобразования данных
│   │
│   ├── features/
│   │   └── build_features.py   # Генерация фичей
│   │
│   ├── models/
│   │   ├── train.py            # Тренировка моделей
│   │   ├── predict.py          # Предикт для prod
│   │   └── utils.py            # Вспомогательные функции
│   │
│   ├── api/
│   │   ├── main.py             # FastAPI сервис
│   │   └── schemas.py          # Pydantic схемы
│   │
│   ├── config.py               # Настройки проекта
│   └── pipeline.py             # Общий pipeline инференса
│
├── artifacts/
│   ├── models/                 # Сохранённые модели (pickle, joblib)
│   ├── scalers/                # Стандартизаторы и энкодеры
│   └── features/               # Генераторы фичей, вспомогательные артефакты
│
├── submissions/                # Сгенерированные сабмиты для Kaggle           
│
├── Dockerfile                  # Docker образ сервиса API
├── requirements.txt            # Все зависимости проекта
└── README.md                   # Текущий файл

6. Дополнительно (опционально)
Если требуется, можно добавить:
список использованных моделей (ARIMA, XGBoost, LightGBM),


анализ сезонности и трендов,


результаты экспериментов,


графики временных рядов,


лучшие параметры моделей.



7. Заключение
Этот README служит центральной точкой входа в проект по прогнозированию спроса на товары.
Он создаёт контекст, описывает данные, цель, метрику и структуру проекта, что позволяет легко подключаться новым участникам или делать дальнейшие улучшения.
